## Todo
- exe or cleaner way to access the app and not in dev mode
- still need to improve optimization (minimizing fetching data and make changes in the update and process stock function, reduce the number of writes on json for static information)
- option for auto delete old cached csv files
- use last fetched when opened so that it runs on previous data unless refreshed or explicitly asked to fetch
- summary: number of stocks, total investment value and an AI generated portfolio summary
- QA with tts